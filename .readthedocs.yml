version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

  commands:
    - python -m pip install -U pip
    - python -m pip install . --group docs

sphinx:
  configuration: docs/sphinx/source/conf.py

formats:
  - htmlzip

submodules:
  exclude: all
