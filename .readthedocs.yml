version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

sphinx:
  configuration: docs/sphinx/source/conf.py

formats:
  - htmlzip

python:
  install:
    - requirements: docs/sphinx/requirements.txt

submodules:
  exclude: all
